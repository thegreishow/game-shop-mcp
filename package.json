{
  "name": "game-shop-mcp",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test:mcp": "tsx scripts/smoke.ts",
    "test:oauth": "tsx scripts/oauth-smoke.ts",
    "verify:oauth-live": "tsx scripts/verify-oauth-live.ts",
    "verify:oauth-runtime-live": "tsx scripts/oauth-live-smoke.ts",
    "test": "npm run typecheck && npm run test:mcp && npm run test:oauth"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "^2.0.0",
    "animejs": "^4.5.0",
    "mcp-handler": "^2.1.1",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "@modelcontextprotocol/client": "^2.0.0",
    "@types/node": "^22.15.8",
    "tsx": "^4.20.5",
    "typescript": "^6.0.0",
    "vercel": "^48.0.0"
  }
}
