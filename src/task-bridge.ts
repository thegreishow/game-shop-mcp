import { cancelExecutionDurable, resolveExecution } from "./execution-store.js";

type McpTaskStatus="working"|"input_required"|"completed"|"failed"|"cancelled";
function taskStatus(status:string):McpTaskStatus{if(status==="completed")return"completed";if(status==="failed")return"failed";if(status==="cancelled")return"cancelled";if(status==="blocked")return"input_required";return"working";}
export async function gameShopTaskGet(executionId:string){const execution=await resolveExecution(executionId);if(!execution)return null;return{taskId:execution.executionId,status:taskStatus(execution.status),createdAt:execution.createdAt,lastUpdatedAt:execution.updatedAt??execution.createdAt,metadata:{extension:"io.modelcontextprotocol/tasks",executionId:execution.executionId,planDigest:execution.planDigest,projectId:execution.projectId??null,completedOperations:execution.completedOperations,totalOperations:execution.totalOperations,lastError:execution.lastError??null},result:execution.status==="completed"?{operationResults:execution.operationResults}:undefined,error:execution.status==="failed"?{message:execution.lastError||"Execution failed"}:undefined};}
export async function gameShopTaskCancel(executionId:string){await cancelExecutionDurable(executionId);return gameShopTaskGet(executionId);}
export function taskBridgeInfo(){return{extension:"io.modelcontextprotocol/tasks",specification:"2026-07-28",mode:"compatibility-bridge",methods:["tasks/get","tasks/update","tasks/cancel"],note:"Game Shop execution handles now map to MCP Task state semantics. Native extension negotiation remains gated on transport/SDK support."};}
