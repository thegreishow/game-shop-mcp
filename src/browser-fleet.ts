export type BrowserMission="explore"|"regression"|"performance"|"repair-loop"|"visual-review";
export function browserFleetInfo(){return{strategy:"mission-routed-browser-fleet",engines:{browserbase:{role:"hosted exploratory browser",configured:Boolean(process.env.BROWSERBASE_API_KEY),bestFor:["explore","visual-review","long-lived-agent-session"]},playwrightMcp:{role:"structured persistent browser MCP",configured:Boolean(process.env.GAME_SHOP_PLAYWRIGHT_MCP_URL),bestFor:["explore","regression","self-healing-qa"]},playwrightCi:{role:"deterministic isolated CI browser",configured:Boolean(process.env.GITHUB_TOKEN),bestFor:["regression","repair-loop","release-gate"]},playwrightCli:{role:"token-efficient coding-agent browser",configured:false,bestFor:["repair-loop","high-throughput-agent"]},chromeDevtools:{role:"deep Chrome diagnostics",configured:Boolean(process.env.GAME_SHOP_CHROME_DEVTOOLS_MCP_URL),bestFor:["performance","console","network","runtime-debugging"]}}};}
export function routeBrowserMission(input:{mission:BrowserMission;preferCloud?:boolean}){const info=browserFleetInfo();const configured=(name:keyof typeof info.engines)=>info.engines[name].configured;let primary:string;let secondary:string|undefined;
switch(input.mission){case"performance":primary=configured("chromeDevtools")?"chrome-devtools-mcp":configured("playwrightMcp")?"playwright-mcp":"playwright-ci";secondary="browserbase";break;case"regression":primary="playwright-ci";secondary=configured("playwrightMcp")?"playwright-mcp":"browserbase";break;case"repair-loop":primary="playwright-cli";secondary="playwright-ci";break;case"visual-review":primary=configured("browserbase")?"browserbase":"playwright-mcp";secondary="playwright-ci";break;default:primary=input.preferCloud!==false&&configured("browserbase")?"browserbase":configured("playwrightMcp")?"playwright-mcp":"playwright-ci";secondary="playwright-ci";}
return{mission:input.mission,primary,secondary,reason:`Route ${input.mission} work to the engine optimized for that evidence type while retaining an independent secondary lane.`,fleet:info};}
