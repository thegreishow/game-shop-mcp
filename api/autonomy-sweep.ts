import { checkIntegrationNetwork } from "../src/integration-health.js";
import { discoverySweep } from "../src/continuous-discovery.js";
import { logEvent } from "../src/governance-ledger.js";
function authorized(request:Request){const expected=process.env.GAME_SHOP_WORKER_SECRET?.trim();const provided=request.headers.get("x-game-shop-worker-secret")?.trim();return Boolean(expected&&provided&&provided===expected);}
export async function POST(request:Request){if(!authorized(request))return new Response(JSON.stringify({error:"Unauthorized worker"}),{status:401,headers:{"content-type":"application/json"}});try{const body=await request.json().catch(()=>({})) as Record<string,unknown>;const capabilities=Array.isArray(body.capabilities)?body.capabilities.map(String).slice(0,12):["browser automation","3d generation","voice","video generation","deployment","payments","database","observability"];const health=await checkIntegrationNetwork({limit:Math.min(20,Math.max(1,Number(body.healthLimit??12)))});const discovery=await discoverySweep({capabilities,limitPerGap:6});await logEvent({type:"autonomy.sweep",data:{healthCounts:health.counts,gaps:discovery.gaps,capabilities}}).catch(()=>{});return new Response(JSON.stringify({health,discovery}),{status:200,headers:{"content-type":"application/json","cache-control":"no-store"}});}catch(e){return new Response(JSON.stringify({error:e instanceof Error?e.message:String(e)}),{status:500,headers:{"content-type":"application/json"}});}}
export async function GET(){return new Response(JSON.stringify({service:"Game Shop Autonomy Sweep",configured:Boolean(process.env.GAME_SHOP_WORKER_SECRET),mode:"integration-health + capability-gap discovery",method:"POST"}),{headers:{"content-type":"application/json"}});}
