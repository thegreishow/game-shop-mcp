import { exchangeAuthorizationCode, oauthBase, oauthClientId } from "../../src/oauth.js";
function json(data:unknown,status=200){return new Response(JSON.stringify(data),{status,headers:{"content-type":"application/json","cache-control":"no-store","pragma":"no-cache"}})}
export async function POST(request:Request){try{const form=await request.formData();const grant=String(form.get("grant_type")||"");const code=String(form.get("code")||"");const clientId=String(form.get("client_id")||"");const redirectUri=String(form.get("redirect_uri")||"");const verifier=String(form.get("code_verifier")||"");if(grant!=="authorization_code")return json({error:"unsupported_grant_type"},400);if(clientId!==oauthClientId())return json({error:"invalid_client"},401);if(!code||!redirectUri||!verifier)return json({error:"invalid_request"},400);return json(await exchangeAuthorizationCode({code,clientId,redirectUri,codeVerifier:verifier,issuer:oauthBase(request)}));}catch(error){return json({error:"invalid_grant",error_description:error instanceof Error?error.message:"Token exchange failed."},400);}}
export async function GET(){return json({error:"method_not_allowed"},405);}
